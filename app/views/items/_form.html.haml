/ <form role="form">
= form_for(@item) do |f|
  = render 'shared/error_messages', object: f.object
  .form-group
    = f.label :name
    %br/
    = f.text_field :name
  .form-group
    = f.label :price
    %br/
    = f.text_field :price
  .form-group
    = f.label :description
    %br/
    = f.text_area :description
  .form-group
    = f.label :image
    = f.file_field :image
    %p.help-block Inserte la imagen del item
  = f.submit
/ </form>